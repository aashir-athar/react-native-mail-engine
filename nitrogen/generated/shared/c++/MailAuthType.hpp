///
/// MailAuthType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::mailengine {

  /**
   * An enum which can be represented as a JavaScript union (MailAuthType).
   */
  enum class MailAuthType {
    PASSWORD      SWIFT_NAME(password) = 0,
    XOAUTH2      SWIFT_NAME(xoauth2) = 1,
    OAUTHBEARER      SWIFT_NAME(oauthbearer) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::mailengine

namespace margelo::nitro {

  // C++ MailAuthType <> JS MailAuthType (union)
  template <>
  struct JSIConverter<margelo::nitro::mailengine::MailAuthType> final {
    static inline margelo::nitro::mailengine::MailAuthType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("password"): return margelo::nitro::mailengine::MailAuthType::PASSWORD;
        case hashString("xoauth2"): return margelo::nitro::mailengine::MailAuthType::XOAUTH2;
        case hashString("oauthbearer"): return margelo::nitro::mailengine::MailAuthType::OAUTHBEARER;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum MailAuthType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::mailengine::MailAuthType arg) {
      switch (arg) {
        case margelo::nitro::mailengine::MailAuthType::PASSWORD: return JSIConverter<std::string>::toJSI(runtime, "password");
        case margelo::nitro::mailengine::MailAuthType::XOAUTH2: return JSIConverter<std::string>::toJSI(runtime, "xoauth2");
        case margelo::nitro::mailengine::MailAuthType::OAUTHBEARER: return JSIConverter<std::string>::toJSI(runtime, "oauthbearer");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert MailAuthType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("password"):
        case hashString("xoauth2"):
        case hashString("oauthbearer"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
